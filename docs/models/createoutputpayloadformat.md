# CreateOutputPayloadFormat

Format to use when sending payload. Defaults to Text.

## Example Usage

```python
from cribl_control_plane.models import CreateOutputPayloadFormat

value = CreateOutputPayloadFormat.TEXT

# Open enum: unrecognized values are captured as UnrecognizedStr
```


## Values

| Name   | Value  |
| ------ | ------ |
| `TEXT` | text   |
| `JSON` | json   |