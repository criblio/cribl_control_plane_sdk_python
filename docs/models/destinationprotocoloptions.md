# DestinationProtocolOptions

Protocol to use when communicating with the destination.

## Example Usage

```python
from cribl_control_plane.models import DestinationProtocolOptions

value = DestinationProtocolOptions.UDP

# Open enum: unrecognized values are captured as UnrecognizedStr
```


## Values

| Name  | Value |
| ----- | ----- |
| `UDP` | udp   |
| `TCP` | tcp   |