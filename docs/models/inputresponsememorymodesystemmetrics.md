# InputResponseMemoryModeSystemMetrics

Select the level of detail for memory metrics

## Example Usage

```python
from cribl_control_plane.models import InputResponseMemoryModeSystemMetrics

value = InputResponseMemoryModeSystemMetrics.BASIC

# Open enum: unrecognized values are captured as UnrecognizedStr
```


## Values

| Name       | Value      |
| ---------- | ---------- |
| `BASIC`    | basic      |
| `ALL`      | all        |
| `CUSTOM`   | custom     |
| `DISABLED` | disabled   |