# OutputLocalSearchStorageMappingType

How event fields are mapped to columns.

## Example Usage

```python
from cribl_control_plane.models import OutputLocalSearchStorageMappingType

value = OutputLocalSearchStorageMappingType.AUTOMATIC

# Open enum: unrecognized values are captured as UnrecognizedStr
```


## Values

| Name        | Value       |
| ----------- | ----------- |
| `AUTOMATIC` | automatic   |
| `CUSTOM`    | custom      |