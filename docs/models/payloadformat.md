# PayloadFormat

Format to use when sending payload. Defaults to Text.

## Example Usage

```python
from cribl_control_plane.models import PayloadFormat

value = PayloadFormat.TEXT

# Open enum: unrecognized values are captured as UnrecognizedStr
```


## Values

| Name   | Value  |
| ------ | ------ |
| `TEXT` | text   |
| `JSON` | json   |