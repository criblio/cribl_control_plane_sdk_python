# SortDirection

Used only when Sort by field is set.

## Example Usage

```python
from cribl_control_plane.models import SortDirection

value = SortDirection.ASC

# Open enum: unrecognized values are captured as UnrecognizedStr
```


## Values

| Name   | Value  |
| ------ | ------ |
| `ASC`  | asc    |
| `DESC` | desc   |