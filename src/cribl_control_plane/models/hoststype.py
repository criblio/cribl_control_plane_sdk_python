"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from cribl_control_plane import models, utils
from cribl_control_plane.types import BaseModel
from cribl_control_plane.utils import validate_open_enum
from enum import Enum
from pydantic import field_serializer
from pydantic.functional_validators import PlainValidator
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class TLS(str, Enum, metaclass=utils.OpenEnumMeta):
    r"""Whether to inherit TLS configs from group setting or disable TLS"""

    INHERIT = "inherit"
    OFF = "off"


class HostsTypeTypedDict(TypedDict):
    host: str
    r"""The hostname of the receiver"""
    port: float
    r"""The port to connect to on the provided host"""
    tls: NotRequired[TLS]
    r"""Whether to inherit TLS configs from group setting or disable TLS"""
    servername: NotRequired[str]
    r"""Servername to use if establishing a TLS connection. If not specified, defaults to connection host (if not an IP); otherwise, uses the global TLS settings."""
    weight: NotRequired[float]
    r"""Assign a weight (>0) to each endpoint to indicate its traffic-handling capability"""


class HostsType(BaseModel):
    host: str
    r"""The hostname of the receiver"""

    port: float
    r"""The port to connect to on the provided host"""

    tls: Annotated[Optional[TLS], PlainValidator(validate_open_enum(False))] = (
        TLS.INHERIT
    )
    r"""Whether to inherit TLS configs from group setting or disable TLS"""

    servername: Optional[str] = None
    r"""Servername to use if establishing a TLS connection. If not specified, defaults to connection host (if not an IP); otherwise, uses the global TLS settings."""

    weight: Optional[float] = 1
    r"""Assign a weight (>0) to each endpoint to indicate its traffic-handling capability"""

    @field_serializer("tls")
    def serialize_tls(self, value):
        if isinstance(value, str):
            try:
                return models.TLS(value)
            except ValueError:
                return value
        return value
