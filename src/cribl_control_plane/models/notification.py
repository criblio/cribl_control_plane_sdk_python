"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .metadataitem import MetadataItem, MetadataItemTypedDict
from .notificationmode import NotificationMode
from .notificationtargetconfig import (
    NotificationTargetConfig,
    NotificationTargetConfigTypedDict,
)
from .notificationtargetdetails import (
    NotificationTargetDetails,
    NotificationTargetDetailsTypedDict,
)
from cribl_control_plane import models
from cribl_control_plane.types import BaseModel, UNSET_SENTINEL
import pydantic
from pydantic import field_serializer, model_serializer
from typing import Any, Dict, List, Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class NotificationTemplateTargetPairTypedDict(TypedDict):
    target_id: str
    r"""The <code>id</code> of the Notification target to send the Notification to."""
    template_id: str
    r"""The <code>id</code> of the Notification template to use."""


class NotificationTemplateTargetPair(BaseModel):
    target_id: Annotated[str, pydantic.Field(alias="targetId")]
    r"""The <code>id</code> of the Notification target to send the Notification to."""

    template_id: Annotated[str, pydantic.Field(alias="templateId")]
    r"""The <code>id</code> of the Notification template to use."""


class NotificationTypedDict(TypedDict):
    condition: str
    r"""The condition that triggers the Notification. Use <code>GET /conditions</code> for a list of supported <code>condition</code> values."""
    conf: Dict[str, Any]
    r"""Configuration for the <code>condition</code> that triggers the Notification. Supported fields vary depending on the <code>condition</code>. Use <code>GET /conditions/{id}</code> to review the configuration for a specific <code>condition</code>."""
    id: str
    r"""Unique identifier."""
    targets: List[str]
    r"""List of the <code>id</code> values for the Notification targets to send the Notification to."""
    disabled: NotRequired[bool]
    r"""If <code>true</code>, the Notification is disabled and the specified condition will not trigger it."""
    group: NotRequired[str]
    r"""The <code>id</code> of the Worker Group or Edge Fleet that the Notification applies to."""
    metadata: NotRequired[List[MetadataItemTypedDict]]
    r"""Metadata tags for the Notification."""
    mode: NotRequired[NotificationMode]
    pack: NotRequired[str]
    r"""The <code>id</code> of the Pack the Notification belongs to. Automatically populated and returned in responses."""
    target_configs: NotRequired[List[NotificationTargetConfigTypedDict]]
    r"""Override settings to apply for each referenced Notification target."""
    target_details: NotRequired[List[NotificationTargetDetailsTypedDict]]
    r"""Additional details about referenced Notification targets. Optionally populated on request."""
    template_target_pairs: NotRequired[List[NotificationTemplateTargetPairTypedDict]]
    r"""If <code>mode</code> is <code>direct</code>, the key-value pairs that define the Notification templates and targets to use for sending Notifications."""


class Notification(BaseModel):
    condition: str
    r"""The condition that triggers the Notification. Use <code>GET /conditions</code> for a list of supported <code>condition</code> values."""

    conf: Dict[str, Any]
    r"""Configuration for the <code>condition</code> that triggers the Notification. Supported fields vary depending on the <code>condition</code>. Use <code>GET /conditions/{id}</code> to review the configuration for a specific <code>condition</code>."""

    id: str
    r"""Unique identifier."""

    targets: List[str]
    r"""List of the <code>id</code> values for the Notification targets to send the Notification to."""

    disabled: Optional[bool] = None
    r"""If <code>true</code>, the Notification is disabled and the specified condition will not trigger it."""

    group: Optional[str] = None
    r"""The <code>id</code> of the Worker Group or Edge Fleet that the Notification applies to."""

    metadata: Optional[List[MetadataItem]] = None
    r"""Metadata tags for the Notification."""

    mode: Optional[NotificationMode] = None

    pack: Optional[str] = None
    r"""The <code>id</code> of the Pack the Notification belongs to. Automatically populated and returned in responses."""

    target_configs: Annotated[
        Optional[List[NotificationTargetConfig]], pydantic.Field(alias="targetConfigs")
    ] = None
    r"""Override settings to apply for each referenced Notification target."""

    target_details: Annotated[
        Optional[List[NotificationTargetDetails]], pydantic.Field(alias="targetDetails")
    ] = None
    r"""Additional details about referenced Notification targets. Optionally populated on request."""

    template_target_pairs: Annotated[
        Optional[List[NotificationTemplateTargetPair]],
        pydantic.Field(alias="templateTargetPairs"),
    ] = None
    r"""If <code>mode</code> is <code>direct</code>, the key-value pairs that define the Notification templates and targets to use for sending Notifications."""

    @field_serializer("mode")
    def serialize_mode(self, value):
        if isinstance(value, str):
            try:
                return models.NotificationMode(value)
            except ValueError:
                return value
        return value

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "disabled",
                "group",
                "metadata",
                "mode",
                "pack",
                "targetConfigs",
                "targetDetails",
                "templateTargetPairs",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    NotificationTemplateTargetPair.model_rebuild()
except NameError:
    pass
try:
    Notification.model_rebuild()
except NameError:
    pass
