"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .errortypestatuspq import ErrorTypeStatusPq, ErrorTypeStatusPqTypedDict
from .healthcounttype import HealthCountType, HealthCountTypeTypedDict
from .healthstringtype import HealthStringType
from cribl_control_plane import models
from cribl_control_plane.types import BaseModel, UNSET_SENTINEL
import pydantic
from pydantic import field_serializer, model_serializer
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class PqTypeStatusTypedDict(TypedDict):
    r"""Persistent queue status information (if persistent queue is enabled)."""

    health: HealthStringType
    health_counts: HealthCountTypeTypedDict
    timestamp: float
    r"""Timestamp (in Unix time) when the persistent queue status was last updated."""
    error: NotRequired[ErrorTypeStatusPqTypedDict]
    r"""Error information for the persistent queue, if applicable."""


class PqTypeStatus(BaseModel):
    r"""Persistent queue status information (if persistent queue is enabled)."""

    health: HealthStringType

    health_counts: Annotated[HealthCountType, pydantic.Field(alias="healthCounts")]

    timestamp: float
    r"""Timestamp (in Unix time) when the persistent queue status was last updated."""

    error: Optional[ErrorTypeStatusPq] = None
    r"""Error information for the persistent queue, if applicable."""

    @field_serializer("health")
    def serialize_health(self, value):
        if isinstance(value, str):
            try:
                return models.HealthStringType(value)
            except ValueError:
                return value
        return value

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["error"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
