"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from cribl_control_plane import models, utils
from cribl_control_plane.types import BaseModel, UNSET_SENTINEL
from enum import Enum
import pydantic
from pydantic import field_serializer, model_serializer
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class BackendID(str, Enum, metaclass=utils.OpenEnumMeta):
    DYNAMIC = "dynamic"
    BYO = "byo"


class SearchExecutionConfigTypedDict(TypedDict):
    backend_id: BackendID
    pool_routing_key: NotRequired[str]
    r"""Selects the ordinary BYO executor pool. Required only when backendId is byo; not used for Lake datasets."""


class SearchExecutionConfig(BaseModel):
    backend_id: Annotated[BackendID, pydantic.Field(alias="backendId")]

    pool_routing_key: Annotated[
        Optional[str], pydantic.Field(alias="poolRoutingKey")
    ] = None
    r"""Selects the ordinary BYO executor pool. Required only when backendId is byo; not used for Lake datasets."""

    @field_serializer("backend_id")
    def serialize_backend_id(self, value):
        if isinstance(value, str):
            try:
                return models.BackendID(value)
            except ValueError:
                return value
        return value

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["poolRoutingKey"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    SearchExecutionConfig.model_rebuild()
except NameError:
    pass
