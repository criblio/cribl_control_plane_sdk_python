"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .healthoptionsstatus import HealthOptionsStatus
from .statuserror import StatusError, StatusErrorTypedDict
from .workerpqstatus import WorkerPQStatus, WorkerPQStatusTypedDict
from cribl_control_plane import models
from cribl_control_plane.types import BaseModel, UNSET_SENTINEL
import pydantic
from pydantic import field_serializer, model_serializer
from typing import Any, Dict, Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class StatusTypeTypedDict(TypedDict):
    r"""Runtime status: health, metrics, and optional persistent-queue info. Fields may be absent when data is unavailable."""

    error: NotRequired[StatusErrorTypedDict]
    health: NotRequired[HealthOptionsStatus]
    r"""Overall health status of the Source or Destination."""
    metrics: NotRequired[Dict[str, Any]]
    r"""Metrics data for the Source or Destination."""
    pq: NotRequired[WorkerPQStatusTypedDict]
    timestamp: NotRequired[int]
    r"""Timestamp (in Unix time) when the status was last updated."""
    use_status_from_lb: NotRequired[bool]
    r"""Set to prefer status from the LB process, not from the worker process."""


class StatusType(BaseModel):
    r"""Runtime status: health, metrics, and optional persistent-queue info. Fields may be absent when data is unavailable."""

    error: Optional[StatusError] = None

    health: Optional[HealthOptionsStatus] = None
    r"""Overall health status of the Source or Destination."""

    metrics: Optional[Dict[str, Any]] = None
    r"""Metrics data for the Source or Destination."""

    pq: Optional[WorkerPQStatus] = None

    timestamp: Optional[int] = None
    r"""Timestamp (in Unix time) when the status was last updated."""

    use_status_from_lb: Annotated[
        Optional[bool], pydantic.Field(alias="useStatusFromLB")
    ] = None
    r"""Set to prefer status from the LB process, not from the worker process."""

    @field_serializer("health")
    def serialize_health(self, value):
        if isinstance(value, str):
            try:
                return models.HealthOptionsStatus(value)
            except ValueError:
                return value
        return value

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            ["error", "health", "metrics", "pq", "timestamp", "useStatusFromLB"]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    StatusType.model_rebuild()
except NameError:
    pass
