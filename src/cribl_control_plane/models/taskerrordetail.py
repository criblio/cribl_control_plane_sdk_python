"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .taskerrorinfo import TaskErrorInfo, TaskErrorInfoTypedDict
from cribl_control_plane.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class TaskErrorDetailTypedDict(TypedDict):
    r"""Task error details. May include a nested <code>reason</code> for wrapped errors and additional properties from the original error."""

    message: str
    r"""Human-readable error message."""
    name: NotRequired[str]
    r"""Error name, if available."""
    reason: NotRequired[TaskErrorInfoTypedDict]
    r"""Serialized error object that describes why a job entered its current <code>state</code>. Includes <code>message</code> and may include a nested <code>reason</code> for wrapped errors."""
    stack: NotRequired[str]
    r"""Truncated stack trace of the error."""


class TaskErrorDetail(BaseModel):
    r"""Task error details. May include a nested <code>reason</code> for wrapped errors and additional properties from the original error."""

    message: str
    r"""Human-readable error message."""

    name: Optional[str] = None
    r"""Error name, if available."""

    reason: Optional[TaskErrorInfo] = None
    r"""Serialized error object that describes why a job entered its current <code>state</code>. Includes <code>message</code> and may include a nested <code>reason</code> for wrapped errors."""

    stack: Optional[str] = None
    r"""Truncated stack trace of the error."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["name", "reason", "stack"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
